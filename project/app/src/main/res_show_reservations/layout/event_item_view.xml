<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="1dp"
    android:background="@color/white">

    <!-- Event text -->
    <TextView
        android:id="@+id/event_date_text"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/primary_orange"
        android:gravity="center"
        android:lineSpacingExtra="2.5dp"
        android:textAllCaps="true"
        android:textColor="@color/white"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintDimensionRatio="H,1:1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintWidth_default="percent"
        app:layout_constraintWidth_percent="0.143"
        tools:text="THU\nJUN 30\n14:00" />

    <!--Colored view divider-->
    <View
        android:layout_width="2dp"
        android:layout_height="0dp"
        android:background="@color/background_calendar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@id/event_date_text"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Info icon -->
    <FrameLayout
        android:id="@+id/event_info_icon_layout"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="@id/event_date_text"
        app:layout_constraintDimensionRatio="W,1:1"
        app:layout_constraintStart_toEndOf="@id/event_date_text"
        app:layout_constraintTop_toTopOf="@id/event_date_text">

        <ImageView
            android:id="@+id/event_info_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:contentDescription="@string/info_icon_description"
            app:srcCompat="@drawable/baseline_info_24"
            app:tint="@color/primary_orange" />

    </FrameLayout>

    <!-- Event information -->
    <LinearLayout
        android:id="@+id/item_layout"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="@id/event_info_icon_layout"
        app:layout_constraintDimensionRatio="W,2.7:1"
        app:layout_constraintStart_toEndOf="@id/event_info_icon_layout"
        app:layout_constraintTop_toTopOf="@id/event_info_icon_layout">

        <!-- Sport name -->
        <TextView
            android:id="@+id/event_sport_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-black"
            android:gravity="center"
            android:textAllCaps="true"
            android:textColor="@color/black"
            android:textSize="16sp"
            tools:text="11-a-side Soccer" />

        <!-- Event Duration -->
        <TextView
            android:id="@+id/event_duration"
            android:layout_width="117dp"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-light"
            android:gravity="center"
            android:textColor="@color/black"
            android:textSize="16sp"
            tools:text="1 hour 30 minutes" />

    </LinearLayout>

    <!-- Show Details button -->
    <Button
        android:id="@+id/show_details_button"
        android:layout_width="110dp"
        android:layout_height="39dp"
        android:text="@string/show_more_button"
        android:textAllCaps="false"
        android:textSize="11sp"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/item_layout"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
